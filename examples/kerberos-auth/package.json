{
  "name": "synthetics-kerberos-auth-example",
  "version": "1.0.0",
  "private": true,
  "description": "Example: Elastic Synthetics HTTP (kerberos/ntlm) and optional browser monitor against Integrated Windows Auth sites",
  "scripts": {
    "test": "npx @elastic/synthetics ."
  },
  "dependencies": {
    "@elastic/synthetics": "*"
  }
}
